import { VisitorRecord } from '../types';

export const MOCK_VISITOR_RECORDS: VisitorRecord[] = [
  // --- AFRICA (primarily South Africa and neighbors) ---
  // Western Cape (Cape Town, Stellenbosch, Franschhoek, Hermanus)
  ...Array(115).fill(null).map((_, i) => ({
    id: `za-wc-ct-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Western Cape',
    city: 'Cape Town',
    checkInMethod: ['QR Code', 'Reception Desk', 'Kiosk'][i % 3] as any,
    guestType: ['First-time', 'Returning', 'VIP'][i % 5 === 0 ? 2 : i % 3 === 0 ? 1 : 0] as any,
  })),
  ...Array(64).fill(null).map((_, i) => ({
    id: `za-wc-sb-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Western Cape',
    city: 'Stellenbosch',
    checkInMethod: ['QR Code', 'Direct Link', 'Reception Desk'][i % 3] as any,
    guestType: ['First-time', 'Returning'][i % 2] as any,
  })),
  ...Array(42).fill(null).map((_, i) => ({
    id: `za-wc-fh-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Western Cape',
    city: 'Franschhoek',
    checkInMethod: 'QR Code' as const,
    guestType: ['First-time', 'VIP'][i % 4 === 0 ? 1 : 0] as any,
  })),
  ...Array(22).fill(null).map((_, i) => ({
    id: `za-wc-hm-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Western Cape',
    city: 'Hermanus',
    checkInMethod: 'Kiosk' as const,
    guestType: 'Returning' as const,
  })),

  // Gauteng (Johannesburg, Pretoria, Sandton, Midrand)
  ...Array(95).fill(null).map((_, i) => ({
    id: `za-gt-jh-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Gauteng',
    city: 'Johannesburg',
    checkInMethod: ['QR Code', 'Reception Desk'][i % 2] as any,
    guestType: ['First-time', 'Returning', 'VIP'][i % 4 === 0 ? 2 : i % 2] as any,
  })),
  ...Array(48).fill(null).map((_, i) => ({
    id: `za-gt-sd-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Gauteng',
    city: 'Sandton',
    checkInMethod: 'Direct Link' as const,
    guestType: 'VIP' as const,
  })),
  ...Array(35).fill(null).map((_, i) => ({
    id: `za-gt-pt-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'Gauteng',
    city: 'Pretoria',
    checkInMethod: 'Reception Desk' as const,
    guestType: 'First-time' as const,
  })),

  // KwaZulu-Natal (Durban, Umhlanga, Ballito)
  ...Array(54).fill(null).map((_, i) => ({
    id: `za-kn-db-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'KwaZulu-Natal',
    city: 'Durban',
    checkInMethod: ['QR Code', 'Kiosk'][i % 2] as any,
    guestType: ['First-time', 'Returning'][i % 2] as any,
  })),
  ...Array(31).fill(null).map((_, i) => ({
    id: `za-kn-uh-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'South Africa',
    region: 'KwaZulu-Natal',
    city: 'Umhlanga',
    checkInMethod: 'QR Code' as const,
    guestType: 'VIP' as const,
  })),

  // Other African Countries (Namibia, Botswana, Zimbabwe)
  ...Array(18).fill(null).map((_, i) => ({
    id: `na-kh-wh-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'Namibia',
    region: 'Khomas',
    city: 'Windhoek',
    checkInMethod: 'Reception Desk' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(12).fill(null).map((_, i) => ({
    id: `bw-se-ga-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'Botswana',
    region: 'South-East',
    city: 'Gaborone',
    checkInMethod: 'QR Code' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(8).fill(null).map((_, i) => ({
    id: `zw-ha-ha-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Africa',
    country: 'Zimbabwe',
    region: 'Harare',
    city: 'Harare',
    checkInMethod: 'Kiosk' as const,
    guestType: 'First-time' as const,
  })),


  // --- EUROPE (Germany, United Kingdom, France, Netherlands) ---
  // Germany (Bavaria-Munich, Berlin-Berlin, Hesse-Frankfurt)
  ...Array(48).fill(null).map((_, i) => ({
    id: `de-by-mu-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'Germany',
    region: 'Bavaria',
    city: 'Munich',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(25).fill(null).map((_, i) => ({
    id: `de-be-be-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'Germany',
    region: 'Berlin',
    city: 'Berlin',
    checkInMethod: 'Direct Link' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(14).fill(null).map((_, i) => ({
    id: `de-he-fr-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'Germany',
    region: 'Hesse',
    city: 'Frankfurt',
    checkInMethod: 'Reception Desk' as const,
    guestType: 'VIP' as const,
  })),

  // United Kingdom (Greater London-London, Scotland-Edinburgh)
  ...Array(52).fill(null).map((_, i) => ({
    id: `uk-gl-lo-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'United Kingdom',
    region: 'Greater London',
    city: 'London',
    checkInMethod: 'QR Code' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(18).fill(null).map((_, i) => ({
    id: `uk-sc-ed-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'United Kingdom',
    region: 'Scotland',
    city: 'Edinburgh',
    checkInMethod: 'Kiosk' as const,
    guestType: 'First-time' as const,
  })),

  // France (Île-de-France-Paris, PACA-Nice)
  ...Array(32).fill(null).map((_, i) => ({
    id: `fr-if-pa-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'France',
    region: 'Île-de-France',
    city: 'Paris',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(10).fill(null).map((_, i) => ({
    id: `fr-pa-ni-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'France',
    region: 'Provence-Alpes-Côte d\'Azur',
    city: 'Nice',
    checkInMethod: 'Direct Link' as const,
    guestType: 'Returning' as const,
  })),

  // Netherlands (North Holland-Amsterdam)
  ...Array(24).fill(null).map((_, i) => ({
    id: `nl-nh-am-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Europe',
    country: 'Netherlands',
    region: 'North Holland',
    city: 'Amsterdam',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),


  // --- NORTH AMERICA (USA, Canada) ---
  // USA (California-San Francisco/LA, New York-NYC, Texas-Austin)
  ...Array(45).fill(null).map((_, i) => ({
    id: `us-ca-sf-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'North America',
    country: 'United States',
    region: 'California',
    city: 'San Francisco',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(30).fill(null).map((_, i) => ({
    id: `us-ny-ny-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'North America',
    country: 'United States',
    region: 'New York',
    city: 'New York City',
    checkInMethod: 'Kiosk' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(20).fill(null).map((_, i) => ({
    id: `us-tx-au-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'North America',
    country: 'United States',
    region: 'Texas',
    city: 'Austin',
    checkInMethod: 'Direct Link' as const,
    guestType: 'VIP' as const,
  })),

  // Canada (Ontario-Toronto, BC-Vancouver)
  ...Array(18).fill(null).map((_, i) => ({
    id: `ca-on-to-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'North America',
    country: 'Canada',
    region: 'Ontario',
    city: 'Toronto',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(12).fill(null).map((_, i) => ({
    id: `ca-bc-va-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'North America',
    country: 'Canada',
    region: 'British Columbia',
    city: 'Vancouver',
    checkInMethod: 'Kiosk' as const,
    guestType: 'Returning' as const,
  })),


  // --- ASIA (India, China, Japan, Singapore) ---
  ...Array(26).fill(null).map((_, i) => ({
    id: `in-mh-mu-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Asia',
    country: 'India',
    region: 'Maharashtra',
    city: 'Mumbai',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(18).fill(null).map((_, i) => ({
    id: `jp-to-to-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Asia',
    country: 'Japan',
    region: 'Tokyo',
    city: 'Tokyo',
    checkInMethod: 'Kiosk' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(15).fill(null).map((_, i) => ({
    id: `sg-sg-sg-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Asia',
    country: 'Singapore',
    region: 'Central Region',
    city: 'Singapore',
    checkInMethod: 'Direct Link' as const,
    guestType: 'VIP' as const,
  })),


  // --- OCEANIA (Australia, New Zealand) ---
  ...Array(22).fill(null).map((_, i) => ({
    id: `au-nsw-sy-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Oceania',
    country: 'Australia',
    region: 'New South Wales',
    city: 'Sydney',
    checkInMethod: 'QR Code' as const,
    guestType: 'Returning' as const,
  })),
  ...Array(10).fill(null).map((_, i) => ({
    id: `nz-au-au-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'Oceania',
    country: 'New Zealand',
    region: 'Auckland',
    city: 'Auckland',
    checkInMethod: 'Reception Desk' as const,
    guestType: 'First-time' as const,
  })),


  // --- SOUTH AMERICA (Brazil, Argentina) ---
  ...Array(14).fill(null).map((_, i) => ({
    id: `br-sp-sp-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'South America',
    country: 'Brazil',
    region: 'São Paulo',
    city: 'São Paulo',
    checkInMethod: 'QR Code' as const,
    guestType: 'First-time' as const,
  })),
  ...Array(8).fill(null).map((_, i) => ({
    id: `ar-ba-ba-${i}`,
    timestamp: new Date(Date.now() - Math.random() * 30 * 24 * 3600 * 1000).toISOString(),
    continent: 'South America',
    country: 'Argentina',
    region: 'Buenos Aires',
    city: 'Buenos Aires',
    checkInMethod: 'Kiosk' as const,
    guestType: 'Returning' as const,
  })),
];
