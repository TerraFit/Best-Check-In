// Update Lost & Found item/status with tenant-bound authorization.
import auth from './_auth.cjs';
const {requireBusinessActor,requireBusinessPermission,resolveTenant,authFailure}=auth;
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'POST, OPTIONS'};
async function audit(u,k,e){try{await fetch(`${u}/rest/v1/audit_logs`,{method:'POST',headers:{apikey:k,Authorization:`Bearer ${k}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify([e])});}catch(e){console.warn('audit failed',e.message);}}
export const handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return{statusCode:405,headers,body:JSON.stringify({error:'Method Not Allowed'})};const a=requireBusinessActor(event);if(!a.ok)return authFailure(a,headers);if(!requireBusinessPermission(a.principal,'canEditLostFound'))return authFailure({status:403,error:'Missing permission: canEditLostFound'},headers);try{const b=JSON.parse(event.body||'{}'),t=resolveTenant(a.principal,b.businessId||b.business_id);if(!t.ok)return authFailure(t,headers);const itemId=b.itemId||b.item_id;if(!itemId)return{statusCode:400,headers,body:JSON.stringify({error:'businessId and itemId are required'})};const u=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_KEY;if(!u||!k)return{statusCode:500,headers,body:JSON.stringify({error:'Server configuration error'})};const sh={apikey:k,Authorization:`Bearer ${k}`,Accept:'application/json','Content-Type':'application/json',Prefer:'return=representation'},base=`${u}/rest/v1/lost_and_found?id=eq.${encodeURIComponent(itemId)}&business_id=eq.${encodeURIComponent(t.businessId)}`,cr=await fetch(`${base}&select=*`,{headers:sh});if(!cr.ok)return{statusCode:cr.status,headers,body:JSON.stringify({error:await cr.text()})};const rows=await cr.json();if(!rows.length)return{statusCode:404,headers,body:JSON.stringify({error:'Item not found'})};const current=rows[0];const allowed=['item_name','description','category','found_date','time_found','room_id','room_number','room_name','booking_id','booking_reference','guest_name','guest_email','guest_phone','storage_location','storage_detail','condition','estimated_value','internal_notes','notes','photo_urls','status','returned_to'];const updates={updated_at:new Date().toISOString()};for(const key of allowed)if(b[key]!==undefined)updates[key]=b[key];if(b.status==='archived'&&!requireBusinessPermission(a.principal,'canDisposeLostFound'))return authFailure({status:403,error:'Missing permission: canDisposeLostFound'},headers);if(['returned','collected'].includes(updates.status))updates.returned_at=updates.returned_at||new Date().toISOString();if(updates.status==='archived')updates.archived_at=new Date().toISOString();const pr=await fetch(base,{method:'PATCH',headers:sh,body:JSON.stringify(updates)});if(!pr.ok)return{statusCode:pr.status,headers,body:JSON.stringify({error:await pr.text()})};const updated=(await pr.json())[0];const empId=b.employee_id||a.principal.employeeId||a.principal.userId||null,empName=b.employee_name||a.principal.email||null;if(b.status&&b.status!==current.status){await fetch(`${u}/rest/v1/lost_and_found_activity`,{method:'POST',headers:sh,body:JSON.stringify([{business_id:t.businessId,item_id:itemId,event_type:b.status==='archived'?'archived':['returned','collected'].includes(b.status)?'returned':'status_change',employee_id:empId,employee_name:empName,from_status:current.status,to_status:b.status,notes:b.note||null}])});await audit(u,k,{business_id:t.businessId,user_id:empId||'00000000-0000-0000-0000-000000000000',user_name:empName||'System',user_role:a.principal.role,action:'lost_found_status_change',description:`Lost & Found ${current.tag_number||itemId}: ${current.status} → ${b.status}`,details:{item_id:itemId,from:current.status,to:b.status},booking_id:current.booking_id||null,guest_name:current.guest_name||null,created_at:new Date().toISOString()});}return{statusCode:200,headers,body:JSON.stringify({success:true,item:updated})};}catch(error){console.error('update-lost-found-item fatal:',error);return{statusCode:500,headers,body:JSON.stringify({error:error.message||'Failed to update item'})}}};
