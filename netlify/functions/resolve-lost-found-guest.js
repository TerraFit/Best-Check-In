// Resolve guest details from a tenant-bound room/booking lookup.
import auth from './_auth.cjs';
const {requireBusinessActor,requireBusinessPermission,resolveTenant,authFailure}=auth;
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'GET, OPTIONS'};
export const handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='GET')return{statusCode:405,headers,body:JSON.stringify({error:'Method Not Allowed'})};const a=requireBusinessActor(event);if(!a.ok)return authFailure(a,headers);if(!requireBusinessPermission(a.principal,'canViewLostFound'))return authFailure({status:403,error:'Missing permission: canViewLostFound'},headers);try{const q=event.queryStringParameters||{},t=resolveTenant(a.principal,q.businessId);if(!t.ok)return authFailure(t,headers);if(!q.roomId&&!q.roomNumber)return{statusCode:400,headers,body:JSON.stringify({error:'businessId and roomId or roomNumber required'})};const u=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_KEY;if(!u||!k)return{statusCode:500,headers,body:JSON.stringify({error:'Server configuration error'})};const sh={apikey:k,Authorization:`Bearer ${k}`,Accept:'application/json'};let room=null;if(q.roomId){const r=await fetch(`${u}/rest/v1/rooms?id=eq.${encodeURIComponent(q.roomId)}&business_id=eq.${encodeURIComponent(t.businessId)}&select=id,room_number,name,room_name`,{headers:sh});room=r.ok?(await r.json())[0]||null:null;}else{const r=await fetch(`${u}/rest/v1/rooms?business_id=eq.${encodeURIComponent(t.businessId)}&room_number=eq.${encodeURIComponent(q.roomNumber)}&select=id,room_number,name,room_name`,{headers:sh});room=r.ok?(await r.json())[0]||null:null;}let bf=`business_id=eq.${encodeURIComponent(t.businessId)}`;if(room?.id)bf+=`&room_id=eq.${encodeURIComponent(room.id)}`;bf+='&status=in.(checked_in,confirmed,completed)';const br=await fetch(`${u}/rest/v1/bookings?${bf}&select=id,guest_name,guest_email,guest_phone,check_in_date,check_out_date,booking_reference,reference,room_id,room_number,status&order=check_in_date.desc&limit=5`,{headers:sh}),bookings=br.ok?await br.json():[],preferred=bookings.find(b=>b.status==='checked_in')||bookings[0]||null;if(!preferred&&!room)return{statusCode:200,headers,body:JSON.stringify({success:true,guest:null})};return{statusCode:200,headers,body:JSON.stringify({success:true,guest:{guest_name:preferred?.guest_name||null,guest_email:preferred?.guest_email||null,guest_phone:preferred?.guest_phone||null,booking_id:preferred?.id||null,booking_reference:preferred?.booking_reference||preferred?.reference||null,check_in_date:preferred?.check_in_date||null,check_out_date:preferred?.check_out_date||null,room_id:room?.id||preferred?.room_id||null,room_number:room?.room_number!=null?String(room.room_number):preferred?.room_number!=null?String(preferred.room_number):q.roomNumber||null,room_name:room?.name||room?.room_name||null}})}catch(error){console.error('resolve-lost-found-guest fatal:',error);return{statusCode:500,headers,body:JSON.stringify({error:error.message||'Failed to resolve guest'})}}};
