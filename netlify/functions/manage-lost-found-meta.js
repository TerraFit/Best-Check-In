// Manage business-specific Lost & Found metadata.
import auth from './_auth.cjs';
const {requireBusinessActor,requireBusinessPermission,resolveTenant,authFailure}=auth;
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'POST, OPTIONS'};
export const handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return{statusCode:405,headers,body:JSON.stringify({error:'Method Not Allowed'})};const a=requireBusinessActor(event);if(!a.ok)return authFailure(a,headers);if(!requireBusinessPermission(a.principal,'canEditLostFound'))return authFailure({status:403,error:'Missing permission: canEditLostFound'},headers);try{let b;try{b=JSON.parse(event.body||'{}')}catch{return{statusCode:400,headers,body:JSON.stringify({error:'Malformed JSON'})}}const t=resolveTenant(a.principal,b.businessId||b.business_id);if(!t.ok)return authFailure(t,headers);const name=(b.name||'').trim(),table=b.action==='add_category'?'lost_and_found_categories':b.action==='add_storage'?'lost_and_found_storage_locations':null;if(!name||!table)return{statusCode:400,headers,body:JSON.stringify({error:'businessId, action, and name required'})};const u=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_KEY;if(!u||!k)return{statusCode:500,headers,body:JSON.stringify({error:'Server configuration error'})};const r=await fetch(`${u}/rest/v1/${table}`,{method:'POST',headers:{apikey:k,Authorization:`Bearer ${k}`,'Content-Type':'application/json',Prefer:'return=representation'},body:JSON.stringify([{business_id:t.businessId,name,is_builtin:false,sort_order:500,active:true}])});if(!r.ok){console.error('manage-lost-found-meta insert failed:',r.status);return{statusCode:500,headers,body:JSON.stringify({error:'Failed to save Lost & Found metadata'})}}const row=(await r.json())[0];return{statusCode:200,headers,body:JSON.stringify({success:true,category:b.action==='add_category'?row:undefined,storage:b.action==='add_storage'?row:undefined})};}catch(error){console.error('manage-lost-found-meta fatal:',error);return{statusCode:500,headers,body:JSON.stringify({error:'Failed to save Lost & Found metadata'})}}};
