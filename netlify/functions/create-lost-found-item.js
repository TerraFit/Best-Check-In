// Create Lost & Found item.
import auth from './_auth.cjs';
const { requireBusinessActor, requireBusinessPermission, resolveTenant, authFailure } = auth;
const headers = { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'Content-Type, Authorization', 'Access-Control-Allow-Methods': 'POST, OPTIONS' };
async function nextTag(u, k, b) { const y = new Date().getFullYear(), sh = { apikey: k, Authorization: `Bearer ${k}`, 'Content-Type': 'application/json', Prefer: 'return=representation' }, r = await fetch(`${u}/rest/v1/lost_and_found_tag_sequences?business_id=eq.${encodeURIComponent(b)}&select=*`, { headers: sh }), rows = r.ok ? await r.json() : []; let seq = 1; if (rows.length && rows[0].year === y) { seq = (rows[0].last_seq || 0) + 1; await fetch(`${u}/rest/v1/lost_and_found_tag_sequences?business_id=eq.${encodeURIComponent(b)}`, { method: 'PATCH', headers: sh, body: JSON.stringify({ last_seq: seq, year: y, updated_at: new Date().toISOString() }) }); } else if (rows.length) { await fetch(`${u}/rest/v1/lost_and_found_tag_sequences?business_id=eq.${encodeURIComponent(b)}`, { method: 'PATCH', headers: sh, body: JSON.stringify({ last_seq: 1, year: y, updated_at: new Date().toISOString() }) }); } else { await fetch(`${u}/rest/v1/lost_and_found_tag_sequences`, { method: 'POST', headers: sh, body: JSON.stringify([{ business_id: b, year: y, last_seq: 1 }]) }); } return `LF-${y}-${String(seq).padStart(4, '0')}`; }
async function audit(u, k, e) { try { await fetch(`${u}/rest/v1/audit_logs`, { method: 'POST', headers: { apikey: k, Authorization: `Bearer ${k}`, 'Content-Type': 'application/json', Prefer: 'return=minimal' }, body: JSON.stringify([e]) }); } catch (e) { console.warn('audit log failed', e.message); } }
export const handler = async (event) => { if (event.httpMethod === 'OPTIONS') return { statusCode: 204, headers, body: '' }; if (event.httpMethod !== 'POST') return { statusCode: 405, headers, body: JSON.stringify({ error: 'Method Not Allowed' }) }; const a = requireBusinessActor(event); if (!a.ok) return authFailure(a, headers); if (!requireBusinessPermission(a.principal, 'canCreateLostFound')) return authFailure({ status: 403, error: 'Missing permission: canCreateLostFound' }, headers); try { const b = JSON.parse(event.body || '{}'), t = resolveTenant(a.principal, b.businessId || b.business_id); if (!t.ok) return authFailure(t, headers); if (!b.item_name) return { statusCode: 400, headers, body: JSON.stringify({ error: 'businessId and item_name are required' }) }; const u = process.env.SUPABASE_URL, k = process.env.SUPABASE_SERVICE_KEY; if (!u || !k) return { statusCode: 500, headers, body: JSON.stringify({ error: 'Server configuration error' }) }; const photos = Array.isArray(b.photo_urls) ? b.photo_urls.filter(Boolean) : [], tag = await nextTag(u, k, t.businessId), now = new Date().toISOString(), today = new Intl.DateTimeFormat('en-CA', { timeZone: 'Africa/Johannesburg', year: 'numeric', month: '2-digit', day: '2-digit' }).format(new Date()); const row = { business_id: t.businessId, tag_number: tag, item_name: b.item_name, description: b.description || null, category: b.category || 'Miscellaneous', found_date: b.found_date || today, time_found: b.time_found || null, room_id: b.room_id || null, room_number: b.room_number != null ? String(b.room_number) : null, room_name: b.room_name || null, booking_id: b.booking_id || null, booking_reference: b.booking_reference || null, guest_name: b.guest_name || null, guest_email: b.guest_email || null, guest_phone: b.guest_phone || null, check_in_date: b.check_in_date || null, check_out_date: b.check_out_date || null, found_by_staff_id: b.found_by_staff_id || null, found_by_staff_name: b.found_by_staff_name || null, storage_location: b.storage_location || null, storage_detail: b.storage_detail || null, condition: b.condition || 'good', estimated_value: b.estimated_value ?? null, internal_notes: b.internal_notes || null, notes: b.notes || null, photo_urls: photos, status: b.status || 'newly_found', created_at: now, updated_at: now }; const r = await fetch(`${u}/rest/v1/lost_and_found`, { method: 'POST', headers: { apikey: k, Authorization: `Bearer ${k}`, 'Content-Type': 'application/json', Prefer: 'return=representation' }, body: JSON.stringify([row]) }); if (!r.ok) return { statusCode: 500, headers, body: JSON.stringify({ error: await r.text() }) }; const item = (await r.json())[0]; await fetch(`${u}/rest/v1/lost_and_found_activity`, { method: 'POST', headers: { apikey: k, Authorization: `Bearer ${k}`, 'Content-Type': 'application/json', Prefer: 'return=minimal' }, body: JSON.stringify([{ business_id: t.businessId, item_id: item.id, event_type: 'created', employee_id: b.found_by_staff_id || a.principal.employeeId || null, employee_name: b.found_by_staff_name || a.principal.email || null, notes: `Item created with tag ${tag}`, details: { tag_number: tag, item_name: b.item_name } }]) }); if (photos.length) await fetch(`${u}/rest/v1/lost_and_found_activity`, { method: 'POST', headers: { apikey: k, Authorization: `Bearer ${k}`, 'Content-Type': 'application/json', Prefer: 'return=minimal' }, body: JSON.stringify([{ business_id: t.businessId, item_id: item.id, event_type: 'photos_added', employee_id: b.found_by_staff_id || a.principal.employeeId || null, employee_name: b.found_by_staff_name || a.principal.email || null, notes: `${photos.length} photo(s) added (${photos.length} total)`, details: { previous_count: 0, new_count: photos.length } }]) }); await audit(u, k, { business_id: t.businessId, user_id: b.found_by_staff_id || a.principal.userId || '00000000-0000-0000-0000-000000000000', user_name: b.found_by_staff_name || a.principal.email || 'System', user_role: a.principal.role, action: 'lost_found_created', description: `Lost & Found item created: ${tag} — ${b.item_name}`, details: { item_id: item.id, tag_number: tag, photos: photos.length }, booking_id: b.booking_id || null, guest_name: b.guest_name || null, created_at: now }); return { statusCode: 200, headers, body: JSON.stringify({ success: true, item }) }; } catch (error) { console.error('create-lost-found-item fatal:', error); return { statusCode: 500, headers, body: JSON.stringify({ error: error.message || 'Failed to create item' }) }; } };
