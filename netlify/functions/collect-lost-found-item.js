// Record guest collection for a Lost & Found item.
import auth from './_auth.cjs';
const {requireBusinessActor,requireBusinessPermission,resolveTenant,authFailure}=auth;
const headers={'Content-Type':'application/json','Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'POST, OPTIONS'};
export const handler=async(event)=>{if(event.httpMethod==='OPTIONS')return{statusCode:204,headers,body:''};if(event.httpMethod!=='POST')return{statusCode:405,headers,body:JSON.stringify({error:'Method Not Allowed'})};const a=requireBusinessActor(event);if(!a.ok)return authFailure(a,headers);if(!requireBusinessPermission(a.principal,'canEditLostFound'))return authFailure({status:403,error:'Missing permission: canEditLostFound'},headers);try{const b=JSON.parse(event.body||'{}'),t=resolveTenant(a.principal,b.businessId||b.business_id);if(!t.ok)return authFailure(t,headers);const itemId=b.itemId||b.item_id,name=(b.collected_by_name||b.collectedByName||'').trim();if(!itemId||!name)return{statusCode:400,headers,body:JSON.stringify({error:'businessId, itemId, and collected_by_name are required'})};const u=process.env.SUPABASE_URL,k=process.env.SUPABASE_SERVICE_KEY;if(!u||!k)return{statusCode:500,headers,body:JSON.stringify({error:'Server configuration error'})};const now=new Date().toISOString(),sh={apikey:k,Authorization:`Bearer ${k}`,'Content-Type':'application/json',Prefer:'return=representation'},base=`${u}/rest/v1/lost_and_found?id=eq.${encodeURIComponent(itemId)}&business_id=eq.${encodeURIComponent(t.businessId)}`,pr=await fetch(base,{method:'PATCH',headers:sh,body:JSON.stringify({status:'collected',returned_at:now,returned_to:name,collected_by_name:name,collected_by_id_number:b.collected_by_id_number||b.collectedByIdNumber||null,collection_signature_url:b.collection_signature_url||b.signatureUrl||null,released_by_staff_id:b.employee_id||b.released_by_staff_id||a.principal.employeeId||null,released_by_staff_name:b.employee_name||b.released_by_staff_name||a.principal.email||null,updated_at:now})});if(!pr.ok)return{statusCode:pr.status,headers,body:JSON.stringify({error:await pr.text()})};const item=(await pr.json())[0];if(!item)return{statusCode:404,headers,body:JSON.stringify({error:'Item not found'})};await fetch(`${u}/rest/v1/lost_and_found_activity`,{method:'POST',headers:sh,body:JSON.stringify([{business_id:t.businessId,item_id:itemId,event_type:'collected',employee_id:a.principal.employeeId||a.principal.userId||null,employee_name:a.principal.email||null,to_status:'collected',notes:`Returned to guest: ${name}`,details:{collected_by_name:name,has_signature:!!item.collection_signature_url}}])});return{statusCode:200,headers,body:JSON.stringify({success:true,item})};}catch(error){console.error('collect-lost-found-item fatal:',error);return{statusCode:500,headers,body:JSON.stringify({error:error.message||'Collection failed'})}}};
