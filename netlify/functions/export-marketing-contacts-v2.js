// Legacy alias: keep the historical v2 route, but execute the hardened
// implementation so there is only one authorization path for marketing exports.
export { handler } from './export-marketing-contacts.js';
