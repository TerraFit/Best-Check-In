import { Calendar, Bed, UserCheck, UserX } from 'lucide-react';

interface Guest { id: string; guest_name?: string; guest_phone?: string; room_number?: string | number | null; room_name?: string | null; check_out_date?: string; }
interface Props { arrivals: Guest[]; stayovers: Guest[]; departures: Guest[]; }
function roomLabel(guest: Guest) { if (guest.room_number === null || guest.room_number === undefined || guest.room_number === '') return guest.room_name || ''; return guest.room_name ? `Room ${guest.room_number} · ${guest.room_name}` : `Room ${guest.room_number}`; }
function GuestRows({ guests, kind }: { guests: Guest[]; kind: 'arrival' | 'stayover' | 'departure' }) { if (!guests.length) return <p className="text-stone-400 text-sm text-center py-4">{kind === 'arrival' ? 'No arrivals today' : kind === 'stayover' ? 'No current stays' : 'No departures today'}</p>; return <div className="space-y-2">{guests.map((guest) => <div key={guest.id} className="p-2 rounded-lg bg-stone-50"><p className="font-medium text-stone-900">{guest.guest_name || 'Guest'}</p>{roomLabel(guest) && <p className="text-xs text-stone-600 mt-0.5">🏨 {roomLabel(guest)}</p>}{kind === 'stayover' && <p className="text-xs text-stone-500 flex items-center gap-1 mt-0.5"><Calendar size={12} /> Check-out: {guest.check_out_date || 'N/A'}</p>}</div>)}</div>; }
export default function EmployeeGuestOverview({ arrivals, stayovers, departures }: Props) { return <div className="space-y-6"><div className="grid grid-cols-1 md:grid-cols-3 gap-4"><div className="bg-white rounded-3xl border border-stone-200 shadow-sm overflow-hidden"><div className="bg-green-50 px-4 py-3 border-b border-green-100 flex items-center gap-2"><UserCheck size={18} className="text-green-600" /><h3 className="font-bold text-sm text-green-800">Arrivées du jour</h3><span className="ml-auto bg-green-100 text-green-800 text-xs font-bold px-2 py-0.5 rounded-full">{arrivals.length}</span></div><div className="p-4"><GuestRows guests={arrivals} kind="arrival" /></div></div><div className="bg-white rounded-3xl border border-stone-200 shadow-sm overflow-hidden"><div className="bg-blue-50 px-4 py-3 border-b border-blue-100 flex items-center gap-2"><Bed size={18} className="text-blue-600" /><h3 className="font-bold text-sm text-blue-800">Séjours en cours</h3><span className="ml-auto bg-blue-100 text-blue-800 text-xs font-bold px-2 py-0.5 rounded-full">{stayovers.length}</span></div><div className="p-4"><GuestRows guests={stayovers} kind="stayover" /></div></div><div className="bg-amber-50 rounded-3xl border border-amber-100 shadow-sm overflow-hidden"><div className="bg-amber-50 px-4 py-3 border-b border-amber-100 flex items-center gap-2"><UserX size={18} className="text-amber-600" /><h3 className="font-bold text-sm text-amber-800">Départs du jour</h3><span className="ml-auto bg-amber-100 text-amber-800 text-xs font-bold px-2 py-0.5 rounded-full">{departures.length}</span></div><div className="p-4"><GuestRows guests={departures} kind="departure" /></div></div></div></div>; }
