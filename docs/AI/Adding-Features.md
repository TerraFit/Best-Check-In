# Adding Features (agent workflow)

1. Confirm Feature Registry entry (or add one in the same PR).  
2. Assign category + minimum package + visibility.  
3. Implement UI with soft-lock if gated.  
4. Implement API enforcement if sensitive.  
5. Update feature doc under `docs/FEATURES/`.  
6. Note Product Decision if commercial meaning changes.  
