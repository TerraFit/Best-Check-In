# Package Permissions

Canonical packages: `starter` | `growth` | `pro` | `business` | `enterprise`

Capacity targets: see Package Matrix (rooms + staff seats).  
Business unlocks multi-establishment.  

Implement checks via a shared helper concept (`checkFeatureAccess`), not scattered `if (plan === 'pro')` without registry keys.
